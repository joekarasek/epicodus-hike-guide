<div>	<a href="#close" title="Close" class="close">X</a>

  <span class="distance">
    <h3>Distance: {{distance place.lat place.lon}} miles</h3>
  </span>
  <h1>{{place.name}}</h1>

  <h2>{{temp-convert place.weather.main.temp}}</h2>
    <span class="descriptionTile">
      <p>{{place.description}}</p>
      <p>{{location-tile place=place}}</p>
    </span>
    <span class="directions">
      <a href="https://maps.google.com?saddr=Current+Location&daddr={{place.lat}},{{place.lon}}">Tap for Directions</a>
    </span>
</div>
