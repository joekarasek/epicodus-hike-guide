import { distance } from 'hike-guide/helpers/distance';
import { module, test } from 'qunit';

module('Unit | Helper | distance');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = distance([42]);
  assert.ok(result);
});
