import { weatherIcon } from 'hike-guide/helpers/weather-icon';
import { module, test } from 'qunit';

module('Unit | Helper | weather icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weatherIcon([42]);
  assert.ok(result);
});
